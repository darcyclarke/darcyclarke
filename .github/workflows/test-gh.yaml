name: "Create npm PR"
on:
  push:
    branches:
      - master
jobs:
  create-pr:
    runs-on: ubuntu-latest
    steps:
      - run: ${{secrets.GITHUB_TOKEN}} | gh login --with-token 
      - run: gh repo clone npm/node
